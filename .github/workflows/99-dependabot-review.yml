---
name: Dependabot review

permissions:
  contents: read
  pull-requests: write

on:
  workflow_call:

jobs:
  review:
    if: github.actor == 'dependabot[bot]'
    runs-on: ubuntu-26.04
    steps:
      - name: Prepare dependency review prompt
        uses: mfranzke/dependabot-review-action@63f46a724b446112e30ddc738853177f8899df07 # v0.2.0
        with:
          github-token: ${{ github.token }}
