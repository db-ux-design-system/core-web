---
"@db-ux/mcp-server": minor
---

Extract MCP server into standalone package (`@db-ux/mcp-server`) with its own versioning, CHANGELOG, and agent rules.
The server can now be installed and invoked independently via `npx --yes @db-ux/mcp-server`.
