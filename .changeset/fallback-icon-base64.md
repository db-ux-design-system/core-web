---
"@db-ux/core-foundations": patch
---

refactor: replaced external fallback icon font as inline base64 to ensure it loads with CSS even when the font file cannot be fetched, preventing layout shifts.
