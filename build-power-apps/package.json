{
  "name": "@db-ui/power-apps-deployment",
  "version": "0.0.0",
  "description": "",
  "workspaces": [
    "*"
  ],
  "scripts": {
    "build:dotnet": " cd DBUI && dotnet build",
    "build:msbuild": " cd DBUI && msbuild /t:build /restore"
  }
}
