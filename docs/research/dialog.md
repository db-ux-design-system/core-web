# DEV Research dialog

## Overview

| Design System                                                                           |                                                                        Component                                                                        | Comment                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --------------------------------------------------------------------------------------- | :-----------------------------------------------------------------------------------------------------------------------------------------------------: | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [Atlassian Design System](https://bitbucket.org/atlassian/atlaskit/src/master/)         |                                            [modal-dialog](https://atlassian.design/components/modal-dialog)                                             | React portal + blanket `div` as backdrop + `div role="dialog"` (`role` switchable to `alertdialog`), JS focus trap, scroll lock and focus restoration; no native `<dialog>`                                                                                                                                                                                                                                                                                          |
| [Bootstrap](https://github.com/twbs/bootstrap)                                          |                                              [modal](https://getbootstrap.com/docs/5.3/components/modal/)                                               | `div.modal` > `div.modal-dialog` > `div.modal-content` (+ `.modal-header`/`.modal-body`/`.modal-footer`); JS sets `role="dialog"` + `aria-modal="true"` on show, renders a `.modal-backdrop` div, traps focus itself; triggered via `data-bs-toggle="modal"` / `data-bs-dismiss="modal"`                                                                                                                                                                             |
| [GitHub Primer](https://github.com/primer/css)                                          |                                                    [dialog](https://primer.style/components/dialog)                                                     | Two implementations: ViewComponents renders a **native `<dialog aria-modal="true">`** with `aria-labelledby`/`aria-describedby`, wrapped in a `<dialog-helper>` custom element; Primer React portals a `div role="dialog" aria-modal` and traps focus with `useFocusTrap`                                                                                                                                                                                            |
| [GitLab Pajamas](https://gitlab.com/gitlab-org/gitlab-services/design.gitlab.com)       |                                                  [modal](https://design.gitlab.com/components/modal/)                                                   | `GlModal` wraps a vendored BootstrapVue `BModal`, so the markup is Bootstrap's; own `setFocus()` orders the close button last; logs a dev warning if neither `title` nor `aria-label` is set                                                                                                                                                                                                                                                                         |
| [HP Enterprise Grommet](https://github.com/grommet/grommet)                             |                                                          [layer](https://v2.grommet.io/layer)                                                           | Styled `div`s with a themed `z-index`, `modal` prop, `position` (center/edges), `full`; blocks the rest of the app via a focus container + `aria-hidden`; no `dialog` element and no `role` by default                                                                                                                                                                                                                                                               |
| [IBM Carbon](https://github.com/carbon-design-system/carbon)                            |                                             [modal](https://carbondesignsystem.com/components/modal/usage/)                                             | `div role="presentation"` wrapper + `div role="dialog" aria-modal="true"`; a native-`<dialog>` based component exists but only as `unstable__Dialog` (not exported, not documented); separate design pattern page splits modal vs. non-modal dialogs                                                                                                                                                                                                                 |
| [KoliBri](https://github.com/public-ui/kolibri)                                         |                                              [dialog](https://public-ui.github.io/docs/components/dialog)                                               | **Native `<dialog>`** in light DOM (`kol-dialog`) with `show(modal)` / `showModal()` / `close()`, `aria-modal` reflecting the mode, `aria-label` (`_variant="blank"`) or `aria-labelledby` to the card heading (`_variant="card"`), required `_label`, `_width`, own scroll lock; the older `kol-modal` is deprecated in favour of it because the dialog element matches the HTML spec; separate `kol-drawer` exists                                                 |
| [Material UI](https://github.com/mui/material-ui)                                       |                                                   [dialog](https://mui.com/material-ui/react-dialog/)                                                   | `Modal` portal → `div role="presentation"` + Paper with `role="dialog"` and `aria-modal="true"`; props `maxWidth`, `fullWidth`, `fullScreen`, `scroll`; own focus trap                                                                                                                                                                                                                                                                                               |
| [MongoDB.design](https://github.com/mongodb/design)                                     |                                              [modal](https://www.mongodb.design/component/modal/code-docs)                                              | Portal + `div role="dialog" aria-modal="true" tabindex="-1"`; extra opinionated variants `confirmation-modal` and `marketing-modal`                                                                                                                                                                                                                                                                                                                                  |
| [Porsche Design System](https://github.com/porsche-design-system/porsche-design-system) |                                         [modal](https://designsystem.porsche.com/v4/components/modal/examples/)                                         | Uses the **native `<dialog>`** internally, so the focus trap comes from the browser; docs state that a11y behaviour therefore varies per browser; the same base powers `sheet`, `flyout` and `drilldown`                                                                                                                                                                                                                                                             |
| [SBB Lyne](https://github.com/lyne-design-system/lyne-components)                       | [dialog](https://digital.sbb.ch/en/design-system/lyne/components/dialog/) / [overlay](https://digital.sbb.ch/en/design-system/lyne/components/overlay/) | Two components on one `SbbOverlayBaseElement`: **Popover API** (`popover="manual"`), own focus trap, `inert`, Escape and scroll-lock controllers, plus an `aria-live` region announcing "Dialog" + title — neither renders a `role="dialog"`. `sbb-dialog` is the centred box (`sbb-dialog-title`/`-content`/`-actions`, `backdrop`, `backdrop-action`); `sbb-overlay` covers the page to darken or hide the background, with a built-in close button and `expanded` |
| [Shopify Polaris](https://github.com/Shopify/polaris)                                   |                            [modal](https://shopify.dev/docs/api/app-home-ui-extension/latest/web-components/overlays/modal)                             | `<s-modal>` web component, opened via `<s-button commandFor="id" command="--show">` (invoker commands with custom `--show`/`--hide`) or `showOverlay()`/`hideOverlay()`; `show`/`hide` (+ `after…`) events; `heading`, `accessibilityLabel` (overrides heading for AT), `size` (5 steps), `padding="base\|none"`, slots `primary-action` + `secondary-actions`; internals undocumented. Documented limits: no opening on page load, always centred, no nesting       |
| [SNCF Design System](https://gitlab.com/SNCF/wcs)                                       |                                        [modal](https://wcs.dev.sncf/?path=/docs/components-modal--documentation)                                        | `wcs-modal` (Stencil, no shadow DOM): `div role="dialog" aria-modal="true"` (`nativeDivDialog` in the source), own tab trap, declarative `show` prop, `wcsDialogClosed` event; `modalTriggerControlsId` is mandatory so focus returns to the trigger, `initialFocusElementId`/`disableAutoFocus` steer it; `size` s–xl, `withoutBackdrop`, `hideActions`, slots `header`/default/`actions`; `showCloseButton` defaults to `false` — and then Escape does not close   |
| [Telefonica Mistica](https://github.com/Telefonica/mistica-web)                         |                                     [dialog](https://mistica-web.vercel.app/?path=/story/components-modals--dialog)                                     | Portal + `FocusTrap` + `div role="dialog"`; only `alert` and `confirm` flavours (no free-form content); can delegate to the platform's own dialog through a native bridge (`shouldRenderNative`)                                                                                                                                                                                                                                                                     |
| [Telekom Scale](https://github.com/telekom/scale)                                       |                                     [modal](https://telekom.github.io/scale/?path=/docs/components-modal--standard)                                     | Stencil web component: backdrop `div` + two `tabindex="0"` sentinel divs as a manual focus trap + `div role="dialog" aria-modal="true" aria-label="{heading}"`; props `size`, `heading`                                                                                                                                                                                                                                                                              |
| [Washington Post Design System](https://build.washingtonpost.com/)                      |                                              [Dialog](https://build.washingtonpost.com/components/dialog/)                                              | backdrop `div` + manual focus trap + `div role="dialog" aria-modal="false" aria-labelledby=""`; huge amount of [fine granular properties](https://build.washingtonpost.com/components/dialog/#API%20Reference); optional close button                                                                                                                                                                                                                                |
| [W3C](https://www.w3.org/WAI/ARIA/apg/patterns/)                                        |                                    [dialog (modal) pattern](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/)                                     | [`role="dialog"` + `aria-modal="true"`](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/examples/dialog/) + accessible name via `aria-labelledby` or `aria-label`, optional `aria-describedby`, contained tab sequence, `Esc` closes; the pattern documents the ARIA construction only and never mentions the native `<dialog>` element — its reference example is still `div role="dialog"` with scripted focus wrapping                                      |

## Conclusion

Three camps:

1. **Native `<dialog>`**: Porsche, Primer ViewComponents and KoliBri. Focus trap, top layer, `Esc` and `::backdrop` (by `closedby`) come from the browser. KoliBri is the clearest signal here: it actively deprecated its old `kol-modal` in favour of a native-`<dialog>` `kol-dialog`.
2. **`div role="dialog" aria-modal="true"` in a portal + hand-written focus trap**: everyone else (Atlassian, Bootstrap, Carbon, GitLab, MUI, MongoDB, Polaris, Mistica, Scale, SNCF). Many of these predate `<dialog>` being Baseline — but not all: SNCF's `wcs-modal` is actively developed, cites the APG dialog pattern in its own source, and still builds the `div` version by hand.
3. **Popover API**: SBB Lyne is the outlier: `popover="manual"` plus its own focus trap, `inert` and Escape handling — for both `sbb-dialog` and `sbb-overlay`, which share one base element.

Group 2 is legacy, not a recommendation — but the W3C does not say so for us. The [APG dialog pattern](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/) still describes only the ARIA construction and ships a `div`-based reference example.

The argument for the native element comes from the [First Rule of ARIA Use](https://www.w3.org/TR/using-aria/#rule1) instead: prefer a native HTML element that already has the semantics and behaviour you need over re-purposing an element with an ARIA role. That matches our own approach to use native platforms functionality. On top of that, there is [no need to trap focus manually in a `<dialog>`](https://css-tricks.com/there-is-no-need-to-trap-focus-on-a-dialog-element/).

### What this means for us

We already ship a native-`<dialog>` implementation: `DBDrawer` uses `showModal()` / `show()`, `requestClose()`, `closedby`, `::backdrop`, invoker commands (`DBButton` with `command="show-modal" | "request-close"` + `commandfor`) and the `supportsClosedBy()` feature detection in `packages/components/src/utils/index.ts`. `DBDrawerHeader` already wires `aria-labelledby` onto the closest `<dialog>`.

So the honest first question is: **do we need a `DBDialog` at all, or is `DBDrawer` with an additional `direction`/`position` value enough?** A centred modal differs from a drawer only in placement and animation; the whole dialog lifecycle is identical, and none of the researched systems keep two separate JS implementations for it (Porsche shares one base across `modal`, `sheet`, `flyout` and `drilldown`). KoliBri does ship `kol-dialog` and `kol-drawer` as separate components, so that split is defensible; but note both wrap the same native element.

SBB Lyne makes the same call more explicitly: `sbb-dialog` (centred box) and `sbb-overlay` (page-covering) are two public components over a single shared base element that owns the whole open/close lifecycle. That is the shape to copy if we want a `DBDialog`: one base, thin components on top, rather than a second implementation.

If we do build a separate component, it should reuse the drawer's base rather than re-implement it (or else, the drawer might migrate to the DBDialog as it's inner element later on):

- native `<dialog>` + `showModal()`, no focus trap of our own
- `closedby="any"` for light dismiss, with the existing ponyfill for browsers in our [Browserslist](../../.browserslistrc) that lack it (`closedby` landed in Chrome 134, Firefox 141; Safari is the laggard)
- `aria-labelledby` from the heading, but we won't set `aria-describedby` to "connect" the body (only possible if we could ensure that the included contents would be very simple, which we can't); same mechanism as `DBDrawerHeader`
- slots for header / content / footer-actions, like Lyne, Carbon and Porsche

Findings / open questions:

- **`alertdialog`**: only Atlassian, MUI, MongoDB and Primer expose it. A native `<dialog>` needs an explicit `role="alertdialog"`. Do we need a `variant="alert"`, or is it enough to document the override?
- **Sizes**: most systems have a width scale (`small`/`medium`/`large`/`xlarge` or `maxWidth` + `fullScreen`); KoliBri instead takes a free-form `_width` capped at `100%`. We could reuse `DrawerContainerSizeList` instead of inventing a second scale.
- **Backdrop click**: Lyne makes it configurable (`backdrop-action="close|none"`), Carbon and MongoDB always close. `closedby="any" | "closerequest"` covers both natively; same as the drawer does today.
- **Non-modal**: Carbon documents modal _and_ non-modal dialogs. `dialog.show()` gives us that for free (the drawer already maps it to `backdrop="none"`).

### To align with concept, business engineering and design

Everything below is a decision the research surfaced but cannot answer on its own. Grouped by who has to answer it. Integrated are the answers that have been aligned.

#### Concept

- **Do we need the component at all?** A centred dialog next to the existing `DBDrawer`, or is a placement variant of the drawer sufficient? Everything else depends on this answer.
  - Yes. Both components slightly differ and after some time and further feedback from the community on functionality, it might even be an option to refactor `drawer` to use `dialog` internally, but today is too early to make an educated decision on this. What we should do in the meantime is even already reusing functionality and styling out of the drawer in an generalized space for both components.
- **Do we need a page-covering variant** (like SBB's `sbb-overlay`) in addition to a centred box, or does `DBDrawer` with `containerSize="full"` already cover that use case?
- **Dismiss rules**: may a dialog be closed by clicking the backdrop, or must the user pick an explicit action? Per use case or one system-wide default? (Lyne makes it configurable, Carbon and MongoDB always close, Shopify closes only via buttons.)
  - We'll make it configurable as well, adapted from `drawer`. We'll only skip the `backdrop=invisible` option.
- **Opinionated confirm/alert flavour or free-form container?** Mistica ships only `alert`/`confirm`, MongoDB ships a dedicated `confirmation-modal`, everyone else takes arbitrary content. This decides whether we need `role="alertdialog"` support.
  - The developers would most likely be able to set this attribute on their own already, but it wouldn't have any visual effect. We'd postponed it from the first phase.
- **Nesting**: Shopify and KoliBri explicitly forbid stacked dialogs, the APG example demonstrates them. Do we forbid, discourage or support them?
  - Undecided in the very beginning.
- **Non-modal dialogs**: do we expose `show()` (page stays interactive) as a supported use case, or modal only?
  - We'll make it configurable as well, adapted from `drawer`.
- **Is a visible heading mandatory?** The APG requires an accessible name; KoliBri makes `_label` required. Do we enforce a heading, or allow a visually hidden label?
- **Is a visible close button mandatory?** The APG strongly recommends one; Scale and Washington Post make it optional. SNCF shows how that goes wrong: `showCloseButton="false"` also disables Escape, leaving no keyboard way out.
  - We won't enforce it, it's a slot available to provide contents in.
- **Content rules for destructive actions**: verb labels instead of "OK"/"Yes", consequences spelled out in the body, and which button gets initial focus (the APG suggests the least destructive one).
  - This wouldn't be part of the first round of the component implementation.
- **Do complex forms belong in a dialog** or on their own page? KoliBri advises against them, Shopify explicitly supports data entry.

#### Design

- **Size scale**: reuse `DrawerContainerSizeList` (`small`/`medium`/`large`/`full`), invent a dialog-specific scale (Shopify has five steps), or allow a free-form width (KoliBri)?
- **Narrow-viewport behaviour**: does the dialog go full-screen on small screens (Carbon, KoliBri, Primer's `position-narrow`), and at which breakpoint?
- **Backdrop**: which strengths do we offer and what is the default? The drawer already has `strong`/`weak`/`invisible`/`none` — SBB additionally distinguishes semi-transparent (keeps orientation) from fully covering.
  - it will be the two (`strong` and `weak`) by `drawer`
- **Placement**: centred only (Shopify cannot be repositioned) or also top/bottom aligned (Primer's `align`)?
  - Keep it simple at the beginning and wait for other use cases by the community.
- **Motion**: enter/exit animation and the reduced-motion variant, including whether the existing drawer animation is reused.
  - Keep it simple at the beginning and wait for other use cases by the community.
- **Footer actions**: order of primary/secondary, wrapping vs. inline on narrow screens (GitLab exposes `keepActionsInline`), and whether a tertiary/cancel slot is needed.
  - we'll expose a footer / end slot that the users could fill with a dialog-footer component.
- **Edge-to-edge content**: do we need a "no padding" escape hatch for images, tables or maps (Shopify's `padding="none"`)?
- **Visual base**: does the dialog reuse `DBCard` styling (KoliBri's `_variant="card"`) or bring its own container, corner radius and elevation?
  - As the DBDialogs styling is adapted / inherited from DBCard, we'll adapt/reference it, but won't use a DBCard exactly as an inner element or even replacement for a DBDialog/`dialog` to keep it flexible.

#### Business engineering

- **Open/close contract**: declarative `open` prop, imperative `showModal()`/`close()`, or invoker commands (`command`/`commandfor`, which `DBButton` already supports)? Shopify went command-first. Which of these do we support and document?
  - We're leaving the 90s behind and will even also go for Invoker Commands, with a simple polyfill for the close button, as we do it in the drawer refactoring.
- **Events**: which events do we emit and which are cancelable? Lyne and Shopify both offer before/after pairs; our drawer currently has `close` and `cancel`.
  - We'll adapt [`close`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLDialogElement/close_event) and [`cancel`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLDialogElement/cancel_event) from the drawer, as those are Web Standards and most flexible already.
- **Return values**: do we expose a result on close (native `returnValue`, Lyne's `assignDialogResult`) so callers can distinguish confirm from cancel?
  - both buttons could be fully controlled by the developers, so these decisions are on them. We'll only suggest to choose techniques like e.g. [`formmethod=dialog`](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/button#formmethod)
- **Forms**: is `<form method="dialog">` supported, and how do submit and validation interact with closing?
  - as we're using the Web Standards, it actually is. We won't expect fully controlled even only, but provide the possibility for synchronization, and that for the `dialog` would emit the regular `close` and `cancel` events. Those will be emitted by the `dialog` natively.
- **Framework parity**: identical behaviour across Angular, React, Vue and Web Components, plus SSR (no `HTMLDialogElement` on the server) and the `display:contents` host quirk we already work around in the drawer.
- **Stacking and top layer**: interaction with tooltip, popover and `DBCustomSelect` — we already carry a documented focus workaround for `<dialog>` and the top layer in `custom-select.lite.tsx`.
  - replacing existing implementations by Popover API etc. in the future has a huge potential to overcome these limitations. But these won't be part of this change.
- **Ponyfill lifetime**: who owns removing the `closedby` fallback once it is evergreen for our [Browserslist](../../.browserslistrc) (there is already a `TODO` in `drawer.scss`)?
  - we'll remove it centrally as soon as our browser list would be fulfilled. We'll communicate this and let the developers decide on appropriate action (either ignore it, because they didn't need to support these browsers anyway anymore, or they will get a mitigation path)
- **Migration**: is there an existing modal in consuming apps or an older DB UX version to migrate from, and does that need a migration guide plus a `major` changeset?
  - yes, but that's business as usual for us.

### Links

- <https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/dialog>
- <https://developer.mozilla.org/en-US/docs/Web/API/HTMLDialogElement/closedBy>
- <https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Reference/Roles/dialog_role>
- <https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/>
- <https://www.w3.org/TR/using-aria/#rule1> (Discontinued Draft; the four rules of ARIA are kept for reference)
- <https://open-ui.org/research/component-matrix/>
