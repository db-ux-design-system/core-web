import type { InputType } from 'storybook/internal/csf';

export const StorybookCardArgTypes: Record<string, InputType> = {
	elevationLevel: { control: 'text' },
	spacing: { control: 'text' },
	behavior: { control: 'text' }
};
