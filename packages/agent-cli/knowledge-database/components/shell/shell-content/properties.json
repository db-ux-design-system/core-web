{
	"component": "shell-content",
	"properties": [
		{
			"name": "children",
			"type": "slot",
			"code": {
				"prop": "children",
				"type": "any"
			},
			"description": "default slot",
			"design": {
				"name": "📦 Children",
				"type": "slot"
			}
		},
		{
			"name": "startSlot",
			"type": "slot",
			"code": {
				"prop": "startSlot",
				"type": "any"
			},
			"description": "Slot for start content, for example add a custom component between an icon and a label.",
			"design": {
				"name": "📦 Start Slot",
				"type": "slot"
			}
		},
		{
			"name": "endSlot",
			"type": "slot",
			"code": {
				"prop": "endSlot",
				"type": "any"
			},
			"description": "Slot for end content, for example add a custom component between an icon and a label.",
			"design": {
				"name": "📦 End Slot",
				"type": "slot"
			}
		},
		{
			"name": "showStartSlot",
			"type": "boolean",
			"code": null,
			"design": {
				"name": "👁️ Show Start Slot"
			}
		},
		{
			"name": "showEndSlot",
			"type": "boolean",
			"code": null,
			"design": {
				"name": "👁️ Show End Slot"
			}
		},
		{
			"name": "variant",
			"type": "enum",
			"values": [
				"auto",
				"fixed"
			],
			"default": "auto",
			"code": {
				"prop": "variant",
				"type": "ShellContentVariantType"
			},
			"description": "Change the scrolling behavior. `auto` scrolling on complete wrapper, `fixed` scrolling only in for main.",
			"design": {
				"name": "🔀 Variant",
				"values": [
					"(Def) Auto",
					"Fixed"
				]
			}
		},
		{
			"name": "mainClass",
			"type": "string",
			"code": {
				"prop": "mainClass",
				"type": "string"
			},
			"description": "Adds `class` to `<main>` element",
			"design": null
		},
		{
			"name": "mainId",
			"type": "string",
			"default": "main-content",
			"code": {
				"prop": "mainId",
				"type": "string"
			},
			"description": "Overrides the default `id` on the `<main>` element.\nThis id is used as the target for the shell skip-navigation link.\nOnly one `DBShellContent` should exist per page to avoid duplicate IDs.",
			"design": null
		},
		{
			"name": "mainLabel",
			"type": "string",
			"code": {
				"prop": "mainLabel",
				"type": "string"
			},
			"description": "Adds `aria-label` to `<main>` element",
			"design": null
		}
	]
}
