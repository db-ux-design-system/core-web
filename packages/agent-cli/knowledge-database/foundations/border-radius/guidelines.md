# Border Radius

Abgerundete Ecken für eine konsistente Formensprache über Komponenten hinweg.

## Regeln

1. Rundungen ausschließlich über `border-radius`-Tokens setzen, nie über feste `px`- oder `rem`-Werte.
2. Für Pill- und Kreisformen `full` verwenden, nicht die größte Stufe der Skala.
